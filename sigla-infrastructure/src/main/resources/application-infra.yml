spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sigla
    username: sigla
    password: sigla
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

sigla:
  storage:
    fiscal-root: var/fiscal
    fiscal-environment: homologacao
